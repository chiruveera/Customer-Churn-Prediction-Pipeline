# OS generated files
.DS_Store

# Python compiled files
__pycache__/
*.pyc

# Virtual environment directory
veera/

# Data files (these are managed by DVC, so Git ignores the data, not the .dvc files)
data/processed/*.csv
data/processed/*.parquet
data/warehouse/*.csv
data/warehouse/*.parquet

# DVC internal directories/files that should be ignored by Git
.dvc/
.dvcignore
